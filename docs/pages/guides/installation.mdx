import { Tabs } from 'nextra/components'

# Installation

Install expo fitbit and required dependencies with `yarn` or `npm`.
```fish
npx expo install @beaverfy/expo-fitbit expo-auth-session
```

## Next Steps
Next, [we'll be intergrating `@beaverfy/expo-fitbit` into our expo project](/guides/getting-started)
